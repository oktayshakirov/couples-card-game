/**
 * RevenueCat — identifiers must match the RevenueCat dashboard exactly
 * (entitlement id, product id in App Store Connect / Play Console).
 */

/** Entitlement that unlocks all decks and removes ads (lifetime). */
export const ENTITLEMENT_LIFETIME = "Love Swipe Full Unlock";

/** Store product id for the one-time lifetime purchase. */
export const PRODUCT_ID_LIFETIME = "com.shadev.loveswipe.lifetime";
